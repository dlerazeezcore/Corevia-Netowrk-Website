# Environment variables for Corevia Network Website.
# Copy this file to `.env` and fill in values as features are added.
# Nothing is required for the current static build.

# APP_URL: The public URL where the site is hosted (used for canonical/OG links).
# APP_URL="https://example.com"
